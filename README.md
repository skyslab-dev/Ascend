# Ascend

An atmospheric endless-flight browser game built with HTML, CSS, and JavaScript.

## Play locally

Open `index.html` in a browser. Cloud leaderboards require Supabase configuration in `leaderboard-config.js` and the schema from `supabase-setup.sql`.

## Tests

Run `node game.test.cjs`.
